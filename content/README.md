# Content Root

This directory contains data-driven game content.

- `base/`: built-in content shipped with the game
- `mods/`: optional override and extension content

Step 01 only bootstraps the folder structure.
